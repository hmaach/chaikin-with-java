## Modern Dark Theme (Recommended)
```java
private static final Color BACKGROUND_COLOR = new Color(24, 24, 36);      // Dark navy
private static final Color TEXT_COLOR = new Color(220, 220, 220);        // Light gray
private static final Color POINT_COLOR = new Color(255, 107, 107);       // Coral red
private static final Color LINE_COLOR = new Color(78, 205, 196);         // Turquoise
```

## Clean Professional
```java
private static final Color BACKGROUND_COLOR = new Color(248, 249, 250);   // Off-white
private static final Color TEXT_COLOR = new Color(52, 58, 64);           // Dark gray
private static final Color POINT_COLOR = new Color(220, 53, 69);         // Bootstrap red
private static final Color LINE_COLOR = new Color(13, 110, 253);         // Bootstrap blue
```

## Vibrant Contrast
```java
private static final Color BACKGROUND_COLOR = new Color(34, 40, 49);      // Dark blue-gray
private static final Color TEXT_COLOR = new Color(238, 238, 238);        // Light gray
private static final Color POINT_COLOR = new Color(255, 165, 0);         // Orange
private static final Color LINE_COLOR = new Color(57, 255, 20);          // Bright green
```

## Elegant Purple Theme
```java
private static final Color BACKGROUND_COLOR = new Color(44, 47, 63);      // Dark purple-gray
private static final Color TEXT_COLOR = new Color(213, 214, 219);        // Light gray
private static final Color POINT_COLOR = new Color(255, 121, 198);       // Pink
private static final Color LINE_COLOR = new Color(189, 147, 249);        // Purple
```

## Ocean Theme
```java
private static final Color BACKGROUND_COLOR = new Color(15, 32, 39);      // Deep blue
private static final Color TEXT_COLOR = new Color(255, 255, 255);        // White
private static final Color POINT_COLOR = new Color(255, 193, 7);         // Golden yellow
private static final Color LINE_COLOR = new Color(32, 201, 151);         // Sea green
```

## Warm Sunset
```java
private static final Color BACKGROUND_COLOR = new Color(46, 52, 64);      // Dark blue-gray
private static final Color TEXT_COLOR = new Color(236, 239, 244);        // Light gray
private static final Color POINT_COLOR = new Color(208, 135, 112);       // Warm brown
private static final Color LINE_COLOR = new Color(235, 203, 139);        // Warm yellow
```

## High Contrast (Accessibility)
```java
private static final Color BACKGROUND_COLOR = Color.WHITE;               // Pure white
private static final Color TEXT_COLOR = Color.BLACK;                    // Pure black
private static final Color POINT_COLOR = new Color(220, 20, 60);        // Crimson
private static final Color LINE_COLOR = new Color(0, 0, 139);           // Dark blue
```